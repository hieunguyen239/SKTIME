@model SKtimeManagement.OutcomeInfo
@{
    ViewBag.Title = "Phiếu chi";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
}

<div class="tabs">
    <div id="heads">
        <a href="@Url.Action("List", "Income")" target="_self" for="main-content" ptitle="Phiếu thu">Phiếu thu</a>
        <span class="active">Phiếu chi</span>
    </div>
</div>
<div class="flexible">
    <div class="w25">
        <div class="group">
            <h3 class="head-line">Tìm kiếm</h3>
            <div id="content">
                <form action="@Url.Action("List", "Outcome")" method="post" for="main-content" ptitle="Phiếu chi">
                    <div>
                        <label>Nhân viên:</label>
                        <span>
                            <select name="EmployeeID" data="@Url.Action("DataList", "Employee")" value="">
                                <option value="">None</option>
                            </select>
                        </span>
                    </div>
                    <div>
                        <label>Mã phiếu:</label><input type="text" class="input" name="Code" value="" />
                    </div>
                    <div>
                        <label>Từ:</label>
                        <div class='input-group date'>
                            <input type='text' class="form-control input" name="From" value="" />
                            <span class="input-group-addon">
                                <span class="glyphicon glyphicon-calendar"></span>
                            </span>
                        </div>
                    </div>
                    <div>
                        <label>Đến:</label>
                        <div class='input-group date'>
                            <input type='text' class="form-control input" name="To" value="" />
                            <span class="input-group-addon">
                                <span class="glyphicon glyphicon-calendar"></span>
                            </span>
                        </div>
                    </div>
                    <div class="text-right">
                        <input type="submit" value="Tìm" url="@Url.Action("List", "Outcome")" class="search"/>
                        <a href="@Url.Action("Create", "Outcome" )" target="_self" class="button add" for="pop-up-content" ptitle="Phiếu chi">Tạo mới</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <div class="w75">
        @Html.Partial(Views.DetailPartial, Model)
    </div>
</div>